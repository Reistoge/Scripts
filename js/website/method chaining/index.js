// method chaning = calling one method after another 
//                  in one continuous line of code.
//                  with method chaining you can do all of this in one line of code

 let userName='ferran'
/*
let letter=userName.charAt(0)
letter=letter.toUpperCase()
letter=letter.trim() 
*/
letter=userName.charAt(0).toUpperCase().trim()
console.log(letter)
